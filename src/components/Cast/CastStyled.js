import styled from "styled-components";

const CastWrapper = styled.div`
  .cast-list {
    list-style: none;
    margin: 0;
    display: flex;
    flex-wrap: wrap;
  }
  .castImg {
    max-height: 450px;
  }
`;

export default CastWrapper;
