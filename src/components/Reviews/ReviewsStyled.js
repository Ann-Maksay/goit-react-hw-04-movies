import styled from "styled-components";

const ReviewsWrapper = styled.div`
  .reviews-list {
    list-style: none;
  }
  .reviews-list-item {
    width: 600px;
  }
`;

export default ReviewsWrapper;
